import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Snap Ticket",
  description: "Generated by Snap Ticket Technology",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
